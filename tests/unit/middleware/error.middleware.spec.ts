import { describe, it, expect } from "vitest";
import "@src/middleware/error.middleware";

describe("Error Middleware", () => {
  it.todo("should catch and handle errors");
  it.todo("should return proper error status codes");
  it.todo("should not expose internal errors to client");
  it.todo("should log errors for debugging");
});
