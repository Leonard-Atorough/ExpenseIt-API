import { describe, it, expect } from "vitest";
import "@src/utils/timeUtils";

describe("Time Utils", () => {
  describe("parseExpiryToMs", () => {
    it.todo("should parse days to milliseconds");
    it.todo("should parse hours to milliseconds");
    it.todo("should parse minutes to milliseconds");
    it.todo("should handle invalid input");
  });
});
